[package]
name = "greeting-plugin"
version = "0.1.0"
edition = "2021"
description = "Example plugin for spec-ai demonstrating custom tool creation"

[lib]
crate-type = ["cdylib"]

[dependencies]
abi_stable = "0.11"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
